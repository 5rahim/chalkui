import Badge      from './Badge'

const { variants, defaultProps } = Badge

const baseStyle = {
   fontFamily: "mono",
   fontSize: "sm",
   px: "0.4em",
   py: "0.1rem",
   borderRadius: "md",
   fontWeight: 600
}

export default {
   baseStyle,
   variants,
   defaultProps,
}
